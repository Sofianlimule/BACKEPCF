{% block body %}
    <h1>Create New List</h1>
    <main>
        <section>
            {{form_start(form)}}
            {{form_row(form.Nom)}}
            <button type="submit">Create</button>
            {{form_end(form)}}
        </section>
    </main>
{% endblock %}
